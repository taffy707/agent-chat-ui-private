# 🏗️ Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                            │
│  (Web Browser, Mobile App, Python Script, cURL, etc.)          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP POST /upload
                             │ multipart/form-data
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FASTAPI APPLICATION                          │
│                      (This Service)                             │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ main.py                                                  │  │
│  │  • POST /upload endpoint                                 │  │
│  │  • File validation (type, size)                          │  │
│  │  • Orchestrates upload → import workflow                 │  │
│  │  • Returns 202 Accepted with document IDs                │  │
│  └──────────────────┬──────────────────────────────────────┘  │
│                     │                                          │
│  ┌──────────────────▼─────────────┐  ┌────────────────────┐  │
│  │ gcs_uploader.py                │  │ config.py          │  │
│  │  • Upload to GCS                │  │  • Load .env       │  │
│  │  • Generate UUID filenames      │  │  • Validate config │  │
│  │  • Return GCS URIs              │  └────────────────────┘  │
│  └──────────────────┬──────────────┘                          │
│                     │                                          │
│  ┌──────────────────▼──────────────────────────────────────┐  │
│  │ vertex_ai_importer.py                                    │  │
│  │  • Trigger import from GCS                               │  │
│  │  • Use INCREMENTAL reconciliation                        │  │
│  │  • Return operation name                                 │  │
│  └──────────────────┬───────────────────────────────────────┘  │
└─────────────────────┼───────────────────────────────────────────┘
                      │
                      │ GCS URI
       ┌──────────────┴───────────────┐
       │                              │
       ▼                              ▼
┌─────────────────┐         ┌────────────────────┐
│ GOOGLE CLOUD    │         │ VERTEX AI SEARCH   │
│ STORAGE         │◄────────┤ DATASTORE          │
│                 │ Reads   │                    │
│ • Stores files  │         │ • Reads from GCS   │
│ • Unique names  │         │ • Extracts text    │
│ • Raw documents │         │ • Chunks docs      │
└─────────────────┘         │ • Generates        │
                            │   embeddings       │
                            │ • Builds index     │
                            └────────────────────┘
```

---

## Request Flow

### 1. Upload Request

```
Client
  │
  │ POST /upload
  │ files: [document.pdf]
  │
  ▼
FastAPI
  │
  │ 1. Validate file
  │    • Check extension (.pdf, .docx, .txt, .html)
  │    • Check MIME type
  │    • Check file size (< 32MB)
  │
  ├─ ❌ Invalid → Return 400 Bad Request
  │
  ├─ ✅ Valid
  │
  ▼
```

### 2. GCS Upload

```
FastAPI
  │
  │ 2. Generate unique filename
  │    • Create UUID: "a1b2c3d4e5f6"
  │    • Prefix original: "a1b2c3d4e5f6_document.pdf"
  │
  ▼
GCS Uploader
  │
  │ 3. Upload to Cloud Storage
  │    • Create blob
  │    • Upload bytes
  │    • Set content type
  │
  ├─ ❌ Upload failed → Log error, continue
  │
  ├─ ✅ Success
  │    • Returns: gs://bucket/a1b2c3d4e5f6_document.pdf
  │
  ▼
```

### 3. Vertex AI Import

```
FastAPI
  │
  │ 4. Trigger Vertex AI import
  │    • GCS URI: gs://bucket/a1b2c3d4e5f6_document.pdf
  │    • Reconciliation: INCREMENTAL
  │
  ▼
Vertex AI Importer
  │
  │ 5. Call Discovery Engine API
  │    • ImportDocumentsRequest
  │    • GcsSource with URIs
  │
  ▼
Vertex AI Search
  │
  │ 6. Start async processing
  │    • Returns operation name
  │    • Background: Extract → Chunk → Embed → Index
  │
  ▼
FastAPI
  │
  │ 7. Return response to client
  │
  ▼
Client receives 202 Accepted
{
  "status": "accepted",
  "documents": [...],
  "vertex_ai_import": {
    "triggered": true,
    "operation_name": "..."
  }
}
```

---

## Component Interactions

### Component Diagram

```
┌────────────────────────────────────────────────────────────┐
│                         main.py                            │
│                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │   Routing    │  │  Validation  │  │   Response   │   │
│  │  /upload     │→ │  File checks │→ │   202 JSON   │   │
│  │  /health     │  │  Size limits │  │   with IDs   │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
│         │                                                 │
│         │ Uses                                            │
│         ▼                                                 │
│  ┌─────────────────────────────────────────────────┐    │
│  │              Service Layer                       │    │
│  │                                                  │    │
│  │  ┌──────────────────┐  ┌────────────────────┐  │    │
│  │  │ GCSUploader      │  │ VertexAIImporter   │  │    │
│  │  │                  │  │                    │  │    │
│  │  │ • upload_file()  │  │ • import_docs()    │  │    │
│  │  │ • delete_file()  │  │ • check_status()   │  │    │
│  │  └──────────────────┘  └────────────────────┘  │    │
│  └─────────────────────────────────────────────────┘    │
│         │                           │                    │
│         │ Depends on                │                    │
│         ▼                           ▼                    │
│  ┌─────────────────────────────────────────────────┐    │
│  │                config.py                         │    │
│  │                                                  │    │
│  │  Settings(BaseSettings)                          │    │
│  │  • GCP_PROJECT_ID                                │    │
│  │  • VERTEX_AI_DATA_STORE_ID                       │    │
│  │  • GCS_BUCKET_NAME                               │    │
│  │  • MAX_FILE_SIZE                                 │    │
│  │  • ALLOWED_EXTENSIONS                            │    │
│  └─────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────┘
```

---

## Data Flow

### Upload Data Flow

```
┌──────────────┐
│ User uploads │
│ document.pdf │
│ (1.2 MB)     │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ FastAPI receives multipart/form-data    │
│                                         │
│ Content-Disposition: form-data;         │
│   name="files"                          │
│   filename="document.pdf"               │
│ Content-Type: application/pdf           │
│                                         │
│ <binary data 1.2MB>                     │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Validation                              │
│ ✓ Extension: .pdf (allowed)             │
│ ✓ MIME type: application/pdf (allowed)  │
│ ✓ Size: 1,258,291 bytes (< 32MB)       │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Generate unique filename                │
│                                         │
│ UUID: a1b2c3d4e5f6                      │
│ Result: a1b2c3d4e5f6_document.pdf       │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Upload to GCS                           │
│                                         │
│ Bucket: metatask-documents-bucket       │
│ Blob: a1b2c3d4e5f6_document.pdf         │
│ URI: gs://metatask-documents-bucket/    │
│      a1b2c3d4e5f6_document.pdf          │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Trigger Vertex AI import                │
│                                         │
│ ImportDocumentsRequest {                │
│   parent: "projects/.../branches/..."   │
│   gcs_source: {                         │
│     input_uris: [                       │
│       "gs://metatask-documents-bucket/  │
│        a1b2c3d4e5f6_document.pdf"       │
│     ]                                   │
│   }                                     │
│   reconciliation_mode: INCREMENTAL      │
│ }                                       │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Vertex AI processes (async)             │
│                                         │
│ 1. Read from GCS                        │
│ 2. Parse PDF                            │
│ 3. Extract text                         │
│ 4. Chunk into segments                  │
│ 5. Generate embeddings                  │
│ 6. Update search index                  │
│                                         │
│ Duration: 5-30 minutes                  │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│ Document indexed and searchable         │
└─────────────────────────────────────────┘
```

---

## Error Handling Flow

```
Client Request
     │
     ▼
┌─────────────────┐
│ File Validation │
└────┬────────────┘
     │
     ├─ Invalid extension
     │  └─→ 400 Bad Request
     │      "Invalid file type: .exe"
     │
     ├─ Invalid MIME type
     │  └─→ 400 Bad Request
     │      "Invalid MIME type: application/octet-stream"
     │
     ├─ File too large
     │  └─→ 400 Bad Request
     │      "File size exceeds maximum"
     │
     ├─ ✓ Valid
     │
     ▼
┌─────────────────┐
│  GCS Upload     │
└────┬────────────┘
     │
     ├─ Permission denied
     │  └─→ Log error
     │      Add to failed_uploads[]
     │      Continue with other files
     │
     ├─ Bucket not found
     │  └─→ Log error
     │      Add to failed_uploads[]
     │      Continue
     │
     ├─ Network error
     │  └─→ Log error
     │      Add to failed_uploads[]
     │      Continue
     │
     ├─ ✓ Success
     │
     ▼
┌─────────────────┐
│ Vertex AI Import│
└────┬────────────┘
     │
     ├─ Datastore not found
     │  └─→ Log error
     │      Return 202 with warning
     │      Files in GCS, import failed
     │
     ├─ API error
     │  └─→ Log error
     ���      Return 202 with warning
     │      Files in GCS, import failed
     │
     ├─ ✓ Success
     │
     ▼
┌─────────────────┐
│ Return Response │
│                 │
│ If all failed:  │
│   400 Bad Req   │
│                 │
│ If some failed: │
│   202 + warning │
│                 │
│ If all success: │
│   202 Accepted  │
└─────────────────┘
```

---

## Authentication Flow

```
┌─────────────────────────────────────────────────────────┐
│                    Authentication                        │
└─────────────────────────────────────────────────────────┘

Development (Local):
┌──────────────────┐
│ gcloud auth      │
│ application-     │
│ default login    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ ~/.config/gcloud/│
│ application_     │
│ default_         │
│ credentials.json │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ FastAPI app uses │
│ default()        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Access to:       │
│ • Cloud Storage  │
│ • Discovery Eng  │
└──────────────────┘

Production (Service Account):
┌──────────────────┐
│ Service Account  │
│ key.json         │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ GOOGLE_          │
│ APPLICATION_     │
│ CREDENTIALS env  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ FastAPI app uses │
│ credentials file │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Access to:       │
│ • Cloud Storage  │
│ • Discovery Eng  │
└──────────────────┘
```

---

## Deployment Architecture

### Local Development
```
┌──────────────────────────────────────┐
│ Your Computer                        │
│                                      │
│  ┌────────────────────────────────┐ │
│  │ Terminal                       │ │
│  │ $ python main.py               │ │
│  │ Uvicorn running on :8000       │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌────────────────────────────────┐ │
│  │ Browser                        │ │
│  │ http://localhost:8000/docs     │ │
│  └────────────────────────────────┘ │
└──────────────┬───────────────────────┘
               │
               │ Authenticated via gcloud
               ▼
┌──────────────────────────────────────┐
│ Google Cloud (Your Project)          │
│                                      │
│  Cloud Storage    Vertex AI Search   │
│       ↓                 ↓            │
│  Upload files    Trigger import      │
└──────────────────────────────────────┘
```

### Cloud Run Deployment
```
┌──────────────────────────────────────┐
│ Internet                             │
└──────────────┬───────────────────────┘
               │
               │ HTTPS
               ▼
┌──────────────────────────────────────┐
│ Google Cloud Load Balancer           │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│ Cloud Run Service                    │
│ • Auto-scaling containers            │
│ • HTTPS endpoints                    │
│ • Managed TLS certificates           │
│                                      │
│  ┌────────────────────────────────┐ │
│  │ FastAPI Container              │ │
│  │ (Your application)             │ │
│  └────────────────────────────────┘ │
└──────────────┬───────────────────────┘
               │
               │ Authenticated via
               │ Service Account
               ▼
┌──────────────────────────────────────┐
│ Google Cloud Services                │
│                                      │
│  ┌──────────────┐  ┌──────────────┐ │
│  │ Cloud Storage│  │ Vertex AI    │ │
│  │              │  │ Search       │ │
│  └──────────────┘  └──────────────┘ │
└──────────────────────────────────────┘
```

---

## Performance Characteristics

### Upload Performance
```
File Size        Upload Time    Processing Time
-------------------------------------------------
< 1 MB          < 1 second     1-5 minutes
1-10 MB         1-3 seconds    5-15 minutes
10-32 MB        3-10 seconds   15-30 minutes
```

### Concurrent Uploads
```
API can handle multiple concurrent uploads
• Each file processed independently
• Failed uploads don't affect successful ones
• Response includes both successes and failures
```

### Vertex AI Processing
```
Background processing happens asynchronously
• API returns immediately (202 Accepted)
• Processing continues in background
• Check status via operation endpoint
• Documents become searchable when complete
```

---

## Scalability

### Vertical Scaling
```
Single Instance Limits:
• Concurrent requests: ~100
• Memory usage: ~512MB base
• CPU usage: Minimal (I/O bound)
```

### Horizontal Scaling
```
Cloud Run Auto-scaling:
• Min instances: 0 (scale to zero)
• Max instances: 100+ (configurable)
• Scale trigger: Request concurrency
• Cold start: ~1-2 seconds
```

### Bottlenecks
```
1. GCS upload bandwidth
   → Solution: Use regional buckets

2. Vertex AI import rate limits
   → Solution: Batch imports

3. File validation
   → Solution: Already fast (in-memory)
```

---

This architecture supports:
✅ High availability
✅ Horizontal scaling
✅ Independent component failures
✅ Asynchronous processing
✅ Clean separation of concerns
